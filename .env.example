# Used by docker-compose to bake InsForge public config into the frontend build.
# URL: `oss_host` in .insforge/project.json; anon key: `npx @insforge/cli secrets get ANON_KEY`
VITE_INSFORGE_URL=
VITE_INSFORGE_ANON_KEY=
